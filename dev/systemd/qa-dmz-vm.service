[Unit]
Description=QA DMZ VM startup
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'systemctl start tailscaled; systemctl start rabbitmq-server; systemctl start ssh'
RemainAfterExit=true

[Install]
WantedBy=multi-user.target

[Unit]
Description=QA Frontend VM startup
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'systemctl start tailscaled; systemctl start rabbitmq-server; systemctl start ssh; systemctl start apache2'
RemainAfterExit=true

[Install]
WantedBy=default.target
